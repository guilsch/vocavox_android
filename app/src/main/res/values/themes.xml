<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Multivoc" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/oxygen</item>
        <item name="android:colorEdgeEffect">@color/overscroll_edge_color</item>
    </style>

    <style name="ButtonStart" parent="android:style/Widget.Button">
        <item name="android:textSize">24sp</item>
        <item name="fontFamily">@font/oxygen_bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/button_start</item>
        <item name="android:height">30dp</item>
        <item name="android:width">200dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:radius">15dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ButtonStandard" parent="android:style/Widget.Button">
        <item name="android:textSize">@dimen/button_std_text_size</item>
        <item name="fontFamily">@font/oxygen_bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/button_start</item>
        <item name="android:height">10dp</item>
        <item name="android:width">100dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:radius">15dp</item>
        <item name="android:gravity">center</item>
    </style>

    <!--    font-->
    <string name="header_text_size" translatable="false">18</string>

    <style name="Theme.Multivoc.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Multivoc.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Multivoc.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>